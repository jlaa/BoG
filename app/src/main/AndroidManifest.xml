<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.edu.ifpe.tads.pdm.bog">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/bogthepenguin"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/bogthepenguin"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".DetailsGameActivity" />
        <activity android:name=".AddGameActivity" />
        <activity android:name=".MinhaListaActivity" />
        <activity android:name=".DetailsMyGameListActivity"></activity>
    </application>

</manifest>